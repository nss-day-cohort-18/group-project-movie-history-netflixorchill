"use strict";

function makeMovieList(movieList) {
	// let $moviesDisplay = 
	// $(`<div></div>`);
}

function movieListForm(movie, movieId) {
	return new Promise(function (resolve, reject){
		// let movieItem = {
		// 	title: movie ? movie.title : ""
		// },
		// form = 
		// 	`<h3>${movieItem.formTitle}</h3>`;
		// 	resolve(form);
	});
}

module.exports = {
	makeMovieList,
	movieListForm
};